What’s a Sidecar?
A sidecar container is commonly used for logging, monitoring, syncing, or networking alongside the main container.

Real-World Use Case
  You could use this pattern in situations like:
  Ensuring database or service is up before launching app.
  Running init scripts (e.g., database migration).
  Using sidecar for logging/monitoring/debugging while the main app runs.